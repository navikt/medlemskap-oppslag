apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: medlemskap-oppslag
  namespace: medlemskap
  labels:
    team: medlemskap
spec:
  azure:
    application:
      enabled: true
  image: {{ image }}
  port: 7070
  liveness:
    path: isAlive
    initialDelay: 30
    timeout: 1
    periodSeconds: 5
    failureThreshold: 10
  readiness:
    path: isReady
    initialDelay: 30
    timeout: 1
  resources:
    limits:
      cpu: 2000m
      memory: 512Mi
    requests:
      cpu: 50m
      memory: 128Mi
  ingresses:
    - https://medlemskap-oppslag.intern.nav.no
  replicas:
    min: 2
    max: 3
    cpuThresholdPercentage: 90
  prometheus:
    enabled: true
    path: /metrics
  secureLogs:
    enabled: true
  webproxy: true
  accessPolicy:
    inbound:
      rules:
        - application: medlemskap-sykepenger-listener
        - application: sparkel-medlemskap
          namespace: tbd
          cluster: prod-fss
    outbound:
      rules:
        - application: udi-proxy
          namespace: medlemskap
          cluster: prod-fss
      external:
        - host: api-gw.oera.no
        - host: login.microsoftonline.com
        - host: udi-proxy.prod-fss-pub.nais.io
  kafka:
    pool: nav-prod

  env:
    - name: SECURITY_TOKEN_SERVICE_URL
      value: https://api-gw.oera.no/sts/SecurityTokenServiceProvider/
    - name: SECURITY_TOKEN_SERVICE_REST_URL
      value: https://api-gw.oera.no/security-token-service
    - name: MEDL2_BASE_URL
      value: https://api-gw.oera.no/medl2
    - name: AAREG_BASE_URL
      value: https://api-gw.oera.no/aareg-services/api
    - name: SAF_BASE_URL
      value: https://saf.prod-fss-pub.nais.io/graphql
    - name: OPPGAVE_BASE_URL
      value: https://api-gw.oera.no/oppgave
    - name: PDL_BASE_URL
      value: https://api-gw.oera.no/pdl-api/graphql
    - name: EREG_BASE_URL
      value: https://api-gw.oera.no/ereg/api
    - name: UDI_BASE_URL
      value: https://udi-proxy.prod-fss-pub.nais.io
  envFrom:
    - secret: medlemskap-oppslag